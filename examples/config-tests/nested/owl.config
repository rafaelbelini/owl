# Nested Config
# This config only applies to tests in this directory
# It inherits .env from parent but can override if needed
#
# To override specific variables, create a new .env file and point to it:
# env=.env.staging

# Inherit env from parent directory
env=../.env
